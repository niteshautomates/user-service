apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  namespace: hotel-ratings  
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ebs-sc
          
